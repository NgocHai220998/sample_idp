<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <script src="/tailwind-3.4.5.js"></script>
  </head>
  <body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="w-full max-w-md bg-white p-8 rounded-lg shadow-lg">
      <h2 class="text-2xl text-center font-semibold text-gray-700 mb-6">SAML IdP</h2>
      <% if @saml_idp_fail_msg %>
        <div id="saml_idp_fail_msg" class="mb-4 p-3 bg-red-100 text-red-700 rounded"><%= @saml_idp_fail_msg %></div>
      <% end %>
      <%= form_tag do %>
        <%= hidden_field_tag("SAMLRequest", params[:SAMLRequest]) %>
        <%= hidden_field_tag("RelayState", params[:RelayState]) %>
        <div class="mb-4">
          <%= label_tag :email, "Email", class: "block text-gray-700 font-semibold" %>
          <%= email_field_tag :email, params[:email], class: "mt-2 w-full p-3 border border-gray-300 rounded focus:outline-none focus:border-blue-500", autocapitalize: "off", autocorrect: "off", autofocus: "autofocus", spellcheck: "false" %>
        </div>
        <div class="mb-6">
          <%= label_tag :password, "Password", class: "block text-gray-700 font-semibold" %>
          <%= password_field_tag :password, params[:password], class: "mt-2 w-full p-3 border border-gray-300 rounded focus:outline-none focus:border-blue-500", autocapitalize: "off", autocorrect: "off", spellcheck: "false" %>
        </div>
        <div>
          <%= submit_tag "Sign in", class: "w-full bg-blue-500 text-white p-3 rounded hover:bg-blue-600 transition duration-200 cursor-pointer" %>
        </div>
      <% end %>
    </div>
  </body>
</html>
